services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - slovicka-data:/data
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DATA_DIR=/data
    restart: unless-stopped

volumes:
  slovicka-data:
